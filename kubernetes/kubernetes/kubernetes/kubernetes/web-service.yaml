apiVersion: v1
kind: Service
metadata:
  name: web-ctf-service
  namespace: ctf-lab
spec:
  type: NodePort
  selector:
    app: web-ctf
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
